# ─── Environment ───────────────────────────────────────────────
ENVIRONMENT=aws

# ─── RDS MySQL Configuration ──────────────────────────────────
# Build the DATABASE_URL from your RDS endpoint, credentials, and DB name.
# Example: mysql+pymysql://<user>:<password>@<rds-endpoint>:3306/<db_name>
DATABASE_URL=mysql+pymysql://outsource_user:CHANGE_ME@your-rds-instance.abcdef123456.us-east-1.rds.amazonaws.com:3306/outsource_db

# Connection-pool tuning (adjust for your RDS instance size)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=1800

# ─── JWT Configuration ────────────────────────────────────────
JWT_SECRET_KEY=your-secret-key-generate-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_HOURS=24

# ─── CORS ─────────────────────────────────────────────────────
# Comma-separated list of allowed origins for your frontend domain(s)
ALLOWED_ORIGINS=https://your-frontend-domain.com

# ─── Google Gemini API ────────────────────────────────────────
GEMINI_API_KEY=your-gemini-api-key
